PROCEDURE TO COMPILE AND RUN A C++ CODE 

g++ main.cpp ==> to compile the code  ==> we obtain a.exe FILE 
./a.exe ==> to run the exe file (executable)

ios::out means you want to open a file in the output mode (for writing data to a file)
ios::in means you want to open a file in the input mode (for extracting data in the file)
ios::app means you want to open a file in the append mode (for writing data to a file after 
the last byte.

THIS WILL CREATE A FOLDER NAMED "Virus" in C Drive 
and fake text files in it constantly "UNTILL U END VSCODE TASK IN TASK-MANAGER"